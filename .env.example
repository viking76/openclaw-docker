# =============================================================================
# OpenClaw All-in-One Configuration
# =============================================================================
# Copy this file to .env and configure as needed.
# Only ANTHROPIC_API_KEY or OPENAI_API_KEY is required to get started.
# =============================================================================

# -----------------------------------------------------------------------------
# Gateway Authentication (choose one)
# -----------------------------------------------------------------------------
# Password auth (user-friendly): enter password in browser when prompted
OPENCLAW_GATEWAY_PASSWORD=

# Token auth (machine-friendly): access via URL with ?token=YOUR_TOKEN
# If neither password nor token is set, a token is auto-generated and printed in logs
# OPENCLAW_GATEWAY_TOKEN=

# -----------------------------------------------------------------------------
# AI Provider API Keys (at least one required)
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GEMINI_API_KEY=
# MISTRAL_API_KEY=

# AI Model (auto-detected based on API key, or set explicitly)
# Examples: anthropic/claude-sonnet-4-20250514, openai/gpt-4o, google/gemini-2.0-flash
# OPENCLAW_MODEL=openai/gpt-4o

# Alternative: skip API key setup (for OAuth authentication later)
# OPENCLAW_AUTH_CHOICE=skip

# -----------------------------------------------------------------------------
# TLS / HTTPS
# -----------------------------------------------------------------------------
# Enable HTTPS with auto-generated self-signed certificate
# OPENCLAW_TLS_ENABLED=true

# For custom certificates, mount them to /certs/cert.pem and /certs/key.pem
# or use Docker secrets (tls_cert, tls_key)

# -----------------------------------------------------------------------------
# SSH Access (optional)
# -----------------------------------------------------------------------------
# Enable SSH server with public key authentication
# OPENCLAW_SSH_ENABLED=true
# OPENCLAW_SSH_PORT=22
# OPENCLAW_SSH_AUTHORIZED_KEYS=ssh-ed25519 AAAA... user@host

# Or mount your authorized_keys file to /ssh/authorized_keys
# Or use Docker secret: ssh_authorized_keys

# -----------------------------------------------------------------------------
# Advanced Options
# -----------------------------------------------------------------------------
# Skip auto-setup on first run (use after manual OAuth setup)
# OPENCLAW_SKIP_ONBOARD=true

# Auto-update OpenClaw to latest version on every container startup
# OPENCLAW_AUTO_UPDATE=true
# Update channel: stable, beta, or dev (default: stable)
# OPENCLAW_UPDATE_CHANNEL=stable
